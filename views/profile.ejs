<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <h1 class="py-5 text-center text-2xl text-pink-600 bold">see your profile page</h1>

    <div class="cards flex gap-7 justify-center flex-wrap px-5">

        <% data.forEach(function(user) { %>

            <div class="bg-white card w-1/5 p-3 rounded border border-black pb-6 hover:shadow-lg transition duration-200">
                <div class="img w-100 h-400">
                    <img src=<%= user.img %> class="w-full h-full object-cover" alt="">
                </div>
                <h1 class="text-2xl mt-2 mt-1 capitalize font-bold"><%= user.name %></h1>
                <h3  class="text-base mb-5 leading-none text-xs">Lorem ipsum dolor sit amet consectetur adipisicing elit. Corrupti, amet! Lorem, ipsum.</h3>
                <a href='/profile/<%= user.id %>' class="border hover:bg-black hover:text-white rounded border-solid border-black px-2 py-1 font-bold">See Profile</a>
            </div>
    
        <% }); %>
    </div>
</body>
</html>